/**
 * undo.js -- Undo removed. Quantity is now directly editable in the
 * item edit modal (pencil icon on any inventory row).
 * Stubs kept so existing imports in app.js do not break.
 */
export function showUndoToast() {}
export function clearUndo()     {}
export function initUndo()      {}
